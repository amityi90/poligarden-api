version: 1.0
runtime: python311
build:
  commands:
    build:
      - python3 -m pip install --no-cache-dir -r requirements.txt
run:
  command: gunicorn run:app --bind 0.0.0.0:8080
  network:
    port: 8080
    env: PORT
